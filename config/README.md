# Config content files

Editable copy served by the API (not compiled into the frontend bundle).

| File | Used by |
|------|---------|
| `welcome.md` | Instructor dashboard **Home → Welcome** |
| `chat_options.md` | (see `docs/chat-options-reference.md`) |

**Welcome:** Markdown plus optional **sanitized HTML** (floated images, etc.). See **`docs/welcome-screen.md`** for examples and allowed tags.
